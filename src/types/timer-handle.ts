/** DOM timer handle (numeric ID in Obsidian/Electron renderer; not NodeJS.Timeout). */
export type WeaveTimerHandle = number;

/** DOM interval handle (numeric ID in Obsidian/Electron renderer). */
export type WeaveIntervalHandle = number;
